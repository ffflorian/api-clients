pre-commit:
  parallel: true
  commands:
    prettier:
      glob: '*.{json,md,yml}'
      run: npx prettier --write {staged_files}
    eslint:
      glob: 'packages/**/*.ts'
      run: npx eslint --fix {staged_files}
